const chooseBrand = (brand) => { //action creator
  return {
    type: 'CHOOSE_BRAND',  //action
    brand
  }
}

const showTweets = (tweets) => {
  return {
    type;
  }
}

export default chooseBrand;
